# StreamRelay — النسخة المُصلَّحة النهائية (اقرأني)

مشروع StreamRelay كامل بعد إصلاح مشاكل تشغيل القنوات واستقرار اللوحة.
ارفعه على GitHub فرع `main`، ثم اضغط «تحديث الآن» في اللوحة ليتزامن
السيرفر الرئيسي والسيرفرات البعيدة تلقائياً.

---

## الإصلاحات في هذه النسخة

1. **حلقة EOF (المشكلة الأصلية):** أُزيل `reconnect_at_eof` الذي كان يجعل FFmpeg
   يعيد الاتصال بلا توقّف فلا يُنتج بثاً ("HLS output not ready"). أُضيف كشف
   لحلقة EOF يُفشل القناة بسرعة بدل التعليق دقيقتين.

2. **دعم روابط Xtream Codes** (`http://host:port/user/pass/id` بدون امتداد):
   متابعة إعادة التوجيه 302، اتصالات مستمرّة، وعدم تطبيق خيارات HLS الخاصة
   على بث mpegts المباشر. وكشف نمط Xtream تلقائياً للقنوات الجديدة.

3. **إقلاع أسرع وحل تعثّر القنوات:** فحص الجاهزية كل 250ms بدل 500ms، واعتبار
   البث بدأ بمجرد ظهور أول مقطع `.ts` على القرص حتى لو تأخّر تحديث الـmanifest.

4. **استقرار اللوحة — حدّ إعادة المحاولة (مهم):** كان الافتراضي محاولات إعادة
   تشغيل **بلا حد**، فالقناة المحظورة/المنتهية تعيد المحاولة للأبد وترفع حمل
   السيرفر حتى تتجمّد اللوحة وتنقطع الجلسة. الآن الافتراضي **10 محاولات** ثم
   تتوقف القناة نهائياً. (لإعادة بلا حد للقنوات الموثوقة: `MAX_RESTART_ATTEMPTS=0`).

> أُزيل خيار `extension_picky` لأنه غير مدعوم في بعض إصدارات FFmpeg.
> كل الخيارات المستخدمة الآن متوافقة مع الإصدارات الشائعة.

---

## الملفات المعدّلة
- `backend/src/services/stream.service.js` — محرّك البث (الإصلاحات 1-4)
- `backend/src/services/channel.service.js` — كشف نمط Xtream
- `backend/src/config/index.js` — الافتراضي 10 محاولات
- `.env.example` — توثيق MAX_RESTART_ATTEMPTS=10
نسخة جاهزة منها أيضاً في مجلد `fixed-files/`.

---

## كيف ترفع التحديث (الطريقة الموصى بها)

من جهاز فيه git وصلاحية رفع للمستودع:

```bash
git clone https://github.com/mmlktahmd4-cmd/streamrelay.git
cd streamrelay
# انسخ محتويات هذا المشروع المُصلّح فوق الملفات، ثم:
git add -A
git commit -m "fix: دعم Xtream + إزالة حلقة EOF + إقلاع أسرع + حدّ إعادة المحاولة"
git push origin main
```

ثم في لوحة السيرفر الرئيسي: **الإعدادات → تحديث الآن**.
الرئيسي يتحدّث، وبعدها يحدّث السيرفرات البعيدة تلقائياً عبر SSH من GitHub.

> ملاحظة: التعديلات التي طُبّقت يدوياً على السيرفر للتجربة سيستبدلها
> `git reset --hard` عند التحديث بنسخة GitHub (نفس الإصلاحات) — لا تعارض.

---

## ملاحظات تشغيل مهمة

- **للمشاهدة في المتصفح:** اجعل صيغة الإخراج (output_format) = **HLS**.
  القنوات بصيغة mpegts تكتب ملف `.ts` ولا تُعرض مباشرة في مشغّل الويب.

- **القنوات المحظورة جغرافياً** (`country-not-allow`) أو منتهية الاشتراك أو
  المحظورة بالـIP: لن تعمل مهما أُعيدت المحاولة — هذه مشكلة من المزوّد لا من
  اللوحة. عطّل `auto_restart` لها، أو استخدم سيرفر بثّ في دولة مسموحة.

- **حدّ الاتصالات:** شغّل قناة واحدة لكل اشتراك Xtream في نفس الوقت (إلا إن
  كان اشتراكك يسمح باتصالات متعددة). تشغيل نسخ كثيرة من نفس الرابط يسبب رفض
  المزوّد (459 / connection limit).

- **بعد إعادة تشغيل الـapi يدوياً:** أعد تشغيل nginx أيضاً (`docker compose
  restart nginx`) لتجنّب خطأ 502، لأن nginx قد يمسك عنوان الحاوية القديم.
  سكربت النشر الرسمي (تحديث GitHub) يعيد الكل بالترتيب الصحيح فلا تتكرر.

## السيرفرات البعيدة
- لكي يحدّثها الرئيسي تلقائياً: يجب أن تكون **مربوطة بالـSSH**.
- لو المستودع **خاص (private):** ضع `GITHUB_TOKEN` بصلاحية قراءة في `.env`.

## تحقّق من التشغيل
```bash
cd /opt/streamrelay
docker compose logs -f worker
```
